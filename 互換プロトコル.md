調べた範囲では、BVE外部通信には「これ1つが標準」というものはありません。実装するなら、**通信路（Serial / UDP / TCP / MQTT …）と、その上に載せるBVE向けフォーマットを完全に分離する設計**にするのが適切です。

また前提を1点修正すると、**Rock_On氏の `Communication.dll` はシリアルではなく UDP/IP の固定バイナリ通信**です。BIDSはそれとは別に、Serial・UDP・TCPを扱える体系です。BIDS作者自身も、既存方式として`Communication.dll`をUDP/IPによる外部出力として挙げています。([Speaker Deck][1])

## 結論：互換対象にすべき既存フォーマット

現時点で私なら、最初から次の6系統をCodecとして用意します。

| フォーマット                                | 主なTransport        |     方向 | 性質             | デファクト度            |
| ------------------------------------- | ------------------ | -----: | -------------- | ----------------- |
| **Communication.dll互換**               | UDP                | BVE→外部 | 固定長バイナリ        | **UDPでは最重要**      |
| **BIDS ASCII v202**                   | Serial / UDP / TCP |    双方向 | ASCIIコマンド      | **既存の汎用規格として最重要** |
| **BIDS Binary framing**               | 主にSerial           |    双方向 | `B64E`+Base64等 | BIDS互換用           |
| **BveSerialOutput互換**                 | Serial             | BVE→外部 | 任意テキスト/CSV     | **Serial出力では最重要** |
| **NumerousControllerInterface (NCI)** | Serial             | 外部→BVE | ASCIIコマンド      | コントローラ入力で有力       |
| **独自Native v1**                       | UDP/TCP/MQTT/WS等   |    双方向 | 新規設計           | 今後の本命             |

実際、現在BveEX向けに公開されている **CommEx** も、Serialについて「Binary / BIDS互換 / SerialOutput互換」、EthernetについてUDPを対応対象にしています。つまり、少なくとも現行BveEX界隈でもこの3系統を互換対象とする考え方は妥当です。([GitHub][2])

---

# 1. Communication.dll互換

これはかなり優先度が高いです。

### Transport

**UDP/IP**

BIDSの`communication` MODはRock_On氏の`Communication.dll`と同じデータ配列を送受信する互換実装であり、デフォルトポートは

**UDP 9032**

です。BIDS公式サイトも「Rock_On様のcommunication.dllと同じデータ配列」と明記しています。([Tralsys][3])

現行ソースでも`UdpClient`を使い、local/remoteとも既定値`9032`です。

### Payload

BIDS互換実装から、実際の構造はかなり正確に復元できます。

先頭は

```text
0xFEFEF0F0
```

で、その後に概ね以下が並びます。

```text
uint32 Header

double Location
float  Speed
int32  Time

float  BC
float  MR
float  ER
float  BP
float  SAP
float  Current

int32  IsDoorClosed
float  SpeedAbs
int32  DeltaT

int32  BrakeHandle
int32  PowerHandle
int32  Reverser
int32  ConstSpeed

int32  BrakeNotches
int32  PowerNotches
int32  ATSCheckPosition
int32  B67Position
int32  CarCount
int32  EmergencyBrakePosition
int32  MaxServiceBrakePosition

int32 Panel[256]
int32 Sound[256]
```

ヘッダ定数、全フィールド、Panel/Sound各256要素はBIDSの互換コードで確認できます。

通常の.NET/Windowsのアラインメントで計算すると、

```text
CommunicationStruct  104 bytes
Panel[256]           1024 bytes
Sound[256]           1024 bytes
--------------------------------
合計                  2152 bytes
```

になります。

これは**仕様からの計算値**です。特にC/C++やESP32側で受けるなら、`struct`をそのままUDPに載せるのではなく、オフセット・little endian・paddingを明示的に定義した方が安全です。

### 評価

UDPの既存互換性を重視するなら、これは**必須**だと思います。

特に、

```text
UDP
 └─ CommunicationDllCodec
```

として独立実装すべきです。

---

# 2. BIDS ASCII v202

「BVE用の汎用外部通信プロトコル」に最も近い既存方式です。

作者の現行ドキュメントは2025年3月にも更新されており、v202の仕様が公開されています。([Gist][4])

基本形は

```text
[Header][Identifier][DataType][RequestNumber][改行]
```

例えば

```text
TRIP52\n
```

です。

ヘッダは固定で

```text
TR
```

Identifierは、

| ID  | 意味       |
| --- | -------- |
| `R` | レバーサー操作  |
| `S` | ワンハンドル操作 |
| `P` | 力行操作     |
| `B` | 制動操作     |
| `K` | キー操作     |
| `I` | 運転情報     |
| `V` | バージョン    |
| `E` | エラー      |

となっています。([Gist][4])

運転情報`I`にはさらに、

```text
C = Spec
E = State
H = Handle
P = Panel
S = Sound
D = Door
```

があります。

Stateだけでも、

```text
位置
速度
時刻
BC
MR
ER
BP
SAP
電流
...
```

が定義されています。Panel/Soundも任意インデックスを取得できます。([Gist][4])

### 強み

Communication.dllと違って、

* BVE→外部
* 外部→BVE
* 必要な値だけ要求
* ハンドル操作
* キー操作
* Panel/Sound取得

まで1つの体系に入っています。

したがって、**既存規格をベースに双方向通信したいならBIDSが第一候補**です。

---

# 3. BIDS over Serial

BIDSはアプリケーション層をSerialでも使用します。

現在のBIDS_Server実装ではSerial既定値は、

```text
19200 baud
ASCII
LF
RTS = ON
DTR = ON
```

です。baud、data bits、parity、newline、encoding等は変更できます。

さらにバイナリデータについては、現在のSerial実装は生バイナリを直接垂れ流すのではなく、

```text
B64E<Base64>\n
```

というフレーミングも持っています。

`B64E`を検出してBase64 decodeする実装までソースで確認できます。

BIDS作者の設計説明でも、

* 文字列によるrequest/response
* 変更通知subscription
* little-endian binary update
* 文字列による操作命令

という複数モードを想定しており、Serialでは通信速度・バッファの都合からbinaryをオプション扱いにしています。([Speaker Deck][1])

---

# 4. BveSerialOutput

これも非常に重要ですが、**「プロトコル」と考えない方がよい**です。

BveSerialOutputは、出力フォーマットをユーザーが自由に設定できるSerial出力プラグインです。作者説明として引用されている資料でも「設定ファイルエディタで出力するテキストのフォーマットを自由に変えられる」とされています。([Qiita][5])

実運用では例えば、

```text
速度,ドア\n
```

あるいは

```text
速度,速度,電流,ドア\n
```

のようなCSV風フォーマットが使われています。([Qiita][6])

したがって、

```text
SerialOutputCodec
```

を固定フォーマットにしてしまうのは間違いです。

例えば、

```text
{speed},{current},{door}\n
```

のような**テンプレート式Codec**として実装するのがよいです。

そうすれば、

```text
{speed}\n
{speed},{door}\n
V={speed};BC={bc};P={power}\r\n
```

などをすべてBveSerialOutput互換として扱えます。

### デファクト評価

**自宅運転台・Arduino系のBVE→マイコンSerial出力では、これが実質的なデファクトの1つ**とみてよいと思います。

ただし「フォーマットのデファクト」は存在せず、

> 可変テキストをSerialで送る

という方式自体がデファクトです。

---

# 5. BIDS over UDP

ここはCommunication.dllと混同しないことが重要です。

BIDSには独立した`udp` MODがあり、

```text
既定port = 14147
宛先未指定 = broadcast
```

で、文字列を送る場合は末尾に`\n`を付けています。

つまりUDPには少なくとも、

```text
UDP:9032
  └─ Communication.dll固定バイナリ

UDP:14147
  └─ BIDS ASCII / BIDS data
```

という**全く別の2系統**があります。

これは両方実装すべきです。

---

# 6. BIDS over TCP

BIDSにはTCP client/server実装も存在します。

既定ポートは

```text
TCP 14147
```

です。

TCP clientの現行コードを見る限り、基本的にはBIDSの改行区切りメッセージをTCP stream上に流します。

接続時には例えば、

```text
TRV202\n
```

を送信し、`TRV...`応答でバージョン確認をしています。サーバ側から`PNxxxx`が返れば別ポートへ接続し直す仕組みもあります。

したがって、

```text
BidsAsciiCodec
```

を作ってしまえば、

```text
SerialTransport
UdpTransport
TcpTransport
```

の3つで共用できます。

### デファクト評価

ただし、**TCPについてBIDSがデファクトになっているとは言いにくい**です。

公開されたBVE用TCPプロトコルとしてはかなり有力ですが、Communication.dllやBveSerialOutputほど広く使われている証拠は確認できませんでした。

---

# 7. NumerousControllerInterface規格

これは「外部マスコン→BVE」の互換性を狙うなら拾っておく価値があります。

NumerousControllerInterfaceは独自Serialプロトコルを公開しています。([GitHub][7])

ASCII＋改行区切りで、

```text
NCIInit
NCIName MyController
NCIPowerCount 5
NCIBreakCount 9
NCIHasReverser True
NCIInitEnd
```

という初期化を行い、実行中は、

```text
NCIPower 3
NCIBreak 2
NCIReverser F
NCIButton ATS,True
```

などを送ります。仕様書そのものが公開されています。

これはBveSerialOutputとは逆方向で、

```text
外部コントローラ → BVE
```

が中心です。

よって、

```text
NciCodec
```

を追加しておく価値があります。

---

# 8. MQTT

ここはかなり探しましたが、**公開されているBVE固有のMQTTプロトコルで、標準・デファクトと呼べるものは確認できませんでした。**

つまり、

```text
bve/speed
bve/panel/123
bve/handle/power
```

のようなtopic体系や、

```json
{"speed": 42.5}
```

のようなJSON schemaについて、既存の統一仕様はありません。

したがってMQTTについては、

> TransportとしてMQTTを用意するが、BVE互換Codecとは切り離す

のが正解です。

例えば自前Native protocolなら、

```text
bve/{instance}/state
bve/{instance}/panel
bve/{instance}/sound
bve/{instance}/command/handle
bve/{instance}/command/key
```

などを定義できます。

MQTT自体はESP32等との連携には便利ですが、BVEのリアルタイム運転台用途ではbrokerを挟むメリットがそれほど大きくないため、**LAN内の実車運転台ならUDP/TCPを先に実装**することを勧めます。

---

# 9. WebSocket / HTTP / REST

これについても、**公開された汎用BVEプロトコルとしてのデファクトは確認できませんでした。**

ただしWebブラウザ・スマホ表示にはかなり相性がよいため、新規Native protocolのTransportとしては価値があります。

最近ではBVE Train Navi SystemがBveEXプラグインとiOS/AndroidアプリをQRペアリングして連携する実例があります。ただし公開情報ではwire protocolまでは仕様化されていません。したがって現時点では互換規格として採用する対象というより、別の独自実装例と考えるべきです。([Bluegill77][8])

---

# 10. WebRTC / SkyWay

BIDSにはこれもありました。

BIDS作者は`SkyWayIF MOD`として、WebRTC Gateway経由での通信を計画・実装しており、BIDSDispWebではスマートフォンとの連動も実験しています。ただし当時から開発中・保守停止状態でした。([Speaker Deck][1])

なので、

**互換対象としては優先度低**

でよいでしょう。


# デファクトを整理すると

「どれを実装すれば既存環境をかなり拾えるか」という観点なら、私はこう評価します。

| 用途                  | 最優先                                |
| ------------------- | ---------------------------------- |
| BVE→外部、Ethernet     | **Communication.dll / UDP**        |
| BVE→Arduino等、Serial | **BveSerialOutput互換**              |
| 双方向Serial           | **BIDS ASCII v202**                |
| 双方向UDP              | **BIDS ASCII + Communication.dll** |
| 双方向TCP              | **BIDS ASCII**                     |
| マスコン→BVE Serial     | **BIDS + NCI**                     |
| MQTT                | 既存規格なし → Native                    |
| WebSocket           | 既存規格なし → Native                    |

したがって、**「BIDSが全部のデファクト」というわけでも、「Communication.dllだけ実装すればよい」わけでもありません。**

ネットワークとSerialで歴史的に別系統が育っています。

---

# 今回のプラグインならこう設計するのを勧めます

ここはかなり重要です。

例えばコード構造を、

```text
                     BveEX / ATSEx
                         │
                         ▼
                  Canonical BVE Model
                         │
            ┌────────────┴────────────┐
            ▼                         ▼
         Codecs                    Transports
  ┌──────────────────┐       ┌─────────────────┐
  │ CommunicationDll │       │ Serial          │
  │ BIDS ASCII       │       │ UDP             │
  │ BIDS Binary      │       │ TCP Client      │
  │ SerialOutput     │       │ TCP Server      │
  │ NCI              │       │ MQTT            │
  │ Native V1        │       │ WebSocket       │
  └──────────────────┘       └─────────────────┘
```

にします。

例えば、

```text
CommunicationDllCodec + UDP
BidsAsciiCodec        + Serial
BidsAsciiCodec        + UDP
BidsAsciiCodec        + TCP
SerialOutputCodec     + Serial
NciCodec              + Serial
NativeCodec           + UDP
NativeCodec           + TCP
NativeJsonCodec       + MQTT
NativeJsonCodec       + WebSocket
```

と自由に組み合わせられるようにします。

**これが「なるべく既存のものすべてを内包する」という目的には一番効きます。**

`UdpCommunicationDll.cs`のようにTransportとCodecを直結してしまうと、後からかなり苦しくなります。

## 実装優先順位

最初のリリースなら、

1. **Communication.dll / UDP 9032**
2. **BveSerialOutput互換 / Serial**
3. **BIDS ASCII v202 / Serial**
4. **BIDS ASCII v202 / UDP 14147**
5. **BIDS ASCII v202 / TCP 14147**
6. **NCI / Serial**
7. Native binary / UDP・TCP
8. MQTT
9. WebSocket

の順がよいと思います。

特に1～5を揃えると、**歴史的なBVE外部機器との互換性と、比較的新しいBIDS系の双方向通信の両方をかなり広く覆えます。**

次に進めるなら、**Communication.dll / BIDS / BveSerialOutput / NCIについて、1 byte単位のwire format、要求/応答、BVE側で取得・操作できる全データ項目を表にして、「今回作る統一内部データモデル」にマッピング**するところまでやるのがよいです。ここを先に決めれば、その後Serial/TCP/UDP/MQTTを増やしてもコア部分を変更せずに済みます。

[1]: https://speakerdeck.com/tetsuotter/bids?utm_source=chatgpt.com "BIDS ～運転ゲームの表示拡張を容易化するために～ - Speaker Deck"
[2]: https://github.com/stop-pattern/CommEx "GitHub - stop-pattern/CommEx: BveEXを通してBveと外部で通信するプラグイン · GitHub"
[3]: https://tralsys.github.io/BIDS/?utm_source=chatgpt.com "BIDSソフトウェア一覧 - Tralsys"
[4]: https://gist.github.com/TetsuOtter/76b974d462276c9c993ddeb0b6a90cb0?utm_source=chatgpt.com "BIDS ASCII Commands Doc · GitHub"
[5]: https://qiita.com/usashirou/items/8d22cedf303128739911?utm_source=chatgpt.com "BVE 鉄道シミュレーターで自宅運転台を動かそう～速度計を動かす #Arduino - Qiita"
[6]: https://qiita.com/usashirou/items/4f5ad751152834698919?utm_source=chatgpt.com "BVE 鉄道シミュレーターで自宅運転台を動かそう~Arduino Unoで動かす #ArduinoUno - Qiita"
[7]: https://github.com/kusaanko/BveNumerousControllerInterface?utm_source=chatgpt.com "GitHub - kusaanko/BveNumerousControllerInterface: Bve用の入力プラグイン · GitHub"
[8]: https://bve.bluegill77.jp/navi?utm_source=chatgpt.com "BVE Train Navi System (iOS/Android) | Bluegill77"
